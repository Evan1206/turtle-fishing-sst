# CONTEXT.md — 專案脈絡與決策紀錄

> 這份檔案是「構思層」的固化。任何 agent(Codex、Cowork、Claude Code）
> 在動手前都應先讀它，理解**為什麼**這樣設計，而不只是**做什麼**（做什麼看 `spec.md`）。
>
> 分工原則：spec.md = 規格與驗收標準（做什麼、什麼算對）；
> 本檔 = 決策理由與陷阱（為什麼、哪裡會錯）；
> findings.md = 驗證結果（Cowork 寫）。

---

## 1. 這個專案在做什麼（一句話）

用三份公開資料的空間交叉，檢驗「海龜出現位置」與「商業漁船作業強度」的空間關係，
並探討這個關係如何隨海表溫度移動。三份資料是：
- **Movebank** — 海龜衛星追蹤軌跡（動物在哪）
- **Global Fishing Watch (GFW)** — 漁船作業強度網格（船在哪作業）
- **NOAA OISST** — 海表溫度（環境背景，MVP 先不做，列 v2）

三者的共通貨幣是 **(緯度, 經度, 日期)**。技術核心 = 把三份不同解析度的資料
重新取樣到同一套時空網格上再疊。這段核心已寫在 `src/align.py`，勿改其邏輯。

---

## 2. 關鍵決策與理由（agent 不要擅自推翻，要改回到人）

### 為什麼選海龜（而非海鳥/鯨豚/鯊魚）
- 海龜是衛星追蹤史上資料最完整的海洋類群之一（1982 年起，涵蓋全部七種、幾乎所有海域）。
- Movebank 有專門的**公開資料倉儲** datarepository.movebank.org，資料經審查、綁定論文、
  對公眾開放，**不需向資料擁有者個別索取**，適合快速起步。
- 海龜與漁業的關係單純且方向明確：**混獲（bycatch）受害者**。延繩釣、刺網、拖網
  是全球海龜最大人為死因之一。這讓「漁業→海龜傷亡」的因果在資料裡是明著的。
- 海龜是變溫動物，分布強受海溫驅動 → 第三層引入 SST 是**生物學上本來就該有的關係**，
  不是硬加的裝飾。三份資料在海龜身上有機咬合。

### 曾考慮海鳥但放棄的理由（重要，避免 agent 誤判「重疊」的意義）
- 海鳥（尤其信天翁）會**主動追漁船**吃丟棄的下雜魚 → 空間重疊代表「吸引」而非「迴避」。
- 海龜不會追船，重疊/迴避的解讀較乾淨。
- **但這個教訓仍適用**：不能從「行為出現（空間重疊）」直接推「意圖/機制」。
  中間的機制才是真相。這正是本專案的認識論主軸（見第 4 節）。

### 為什麼用 Python（而非 R）
- 三源在 R 都有第一方套件（gfwr、move2、rerddap），R 起步略快。
- 仍選 Python，理由與專案本身無關：Python 的地理空間疊合技術棧
  （xarray、geopandas、regionmask）是「多源資料對齊」的通用肌肉，遷移性高。
- 唯一較弱的 Movebank 讀取，用 REST API 包一個函式即可解決。

### 資料可得性先於假說（與平常習慣相反，特別注意）
- 一般習慣是「先定假說再找資料」。本專案**反過來**：
  因為 Movebank 每筆 study 由資料擁有者決定開放程度，
  所以要**先在 Movebank 地圖找到一筆公開、海洋物種、地理時間與 GFW（2017 後）重疊的 study，
  再據此定題**。找不到完美吻合的，就調整 config 的 BBOX 去遷就資料。

### Movebank study 選定（2026-07-26，構思層決定）
- **study_id = 1417866900**，「Post-nesting migrations of loggerhead sea turtles nesting in Japan」。
  已寫入 `src/config.py`。
- 選定過程：查遍 Movebank Data Repository（含用學名 Caretta caretta / Chelonia mydas /
  Lepidochelys / Eretmochelys / Dermochelys 交叉搜尋），全球公開海龜研究僅 5 筆，
  其中**只有這一筆**地理落在北太平洋 BBOX（120–180°E, 15–45°N）。物種 100% Caretta caretta
  （符合 Cheloniidae 要求）、CC0 授權、12 隻個體、17360 個定位點，87% 落在 BBOX 內。
  **這是唯一候選，不是眾多選項中挑出來的**，後續若要換 study 需重新走一次這個搜尋過程確認無其他選項。
- **日期範圍已從 2018-01-01~2019-12-31 調整為 2017-01-01~2018-12-31**（同步更新
  `src/config.py` 的 `DATE_START`/`DATE_END`）：原窗口只涵蓋此 study 3 隻個體、2446 個
  定位點；調整後涵蓋 8 隻個體、9390 個定位點，仍在 GFW 資料可靠期（2017 年後）內。
  `data/raw/gfw_effort.parquet` 目前是舊日期窗口（且僅一個月）的連線測試殘留，
  日期範圍改變後 Codex 需要重新用完整 `DATE_START`~`DATE_END` 執行 `fetch_gfw.py`。
- **已知限制（記録供最終結論措辭參考，對應 spec 驗收標準 D）**：即使用調整後的窗口，
  貢獻資料的仍只有 8 隻個體。這代表任何空間關聯結果都可能只反映少數個體的移動偏好，
  不必然是族群層級訊號——這不是能靠換資料源解決的限制，是本專案這個資料規模的天花板，
  最終結論寫作時應保守措辭、註明樣本為個位數隻個體的追蹤資料。
- 若要拓寬到全部 12 隻（涵蓋 2015-2019），會把 2015-2016 的海龜點納入分析，但那段時間
  早於 GFW 資料可靠期，該時段「無漁船資料」是「無觀測」而非「零作業」，混用會踩到
  第 4 節認識論主軸提到的「宣稱指標與代理指標裂縫」——已決定不採用，維持 2017-2018。

### 新增模組 F：互動式靜態展示站（2026-07-27，人 + 構思層決定）
- 人希望把分析結果發布成「即時 UI/UX 動態資料網站」。討論後確認：海龜資料是
  已結束的歷史追蹤（標籤 2015-2018 全部停止傳輸），不存在「即時海龜位置」，
  真正能即時的只有 GFW 一側；若做成真後端排程的即時系統，會變成「漁船即時、
  海龜歷史」兩個時間軸疊在一起的不對稱畫面，複雜度與維運成本都高很多。
  **已決定採用方案是靜態互動展示站**：資料是這批 2017-2018 的固定快照，用
  Leaflet 地圖 + 時間滑桿做出「看起來動態、實際是預先算好的資料」的體驗，
  不需要後端伺服器、不需要資料庫。要更新資料就重新跑 pipeline 再重新部署。
- 詳細契約（資料格式、前端功能、視覺設計規範）已寫入 spec.md 模組 F。
- **視覺品質是明確驗收項,不是風格建議**：人要求「畫面不要太粗糙」，spec.md
  模組 F 已內嵌具體色階（連續藍色 sequential ramp，非彩虹）、中性色/字體 token、
  圖例與 hover 回饋等要求，源自 `dataviz` skill 的驗證方法論。Cowork/人審查時
  對照 spec 那份「視覺設計規範」清單逐項核對，不是主觀評分。
- **前置阻塞**：模組 F 接真資料與對外部署，都要等模組 E（`analyze.py`）驗收通過
  後才進行；目前 `analyze.py` 的狀況待人跟 Codex 確認（見下方進度）。前端骨架
  可以用假資料先開發，不受此阻塞。
- **部署動作**：不管是 Codex 或 Claude Code，都不會自行把網站發布到公開網址，
  這一步永遠要先取得人的明確同意（呼應第 5 節分工原則裡「重大改動要問人」）。

### 模組 F 視覺風格：可愛化（2026-07-27，人決定）
- 人要求海洋、海龜、漁船的視覺呈現要「可愛一點」。已寫入 spec.md 模組 F，
  原則是**資料編碼與裝飾分開處理**：
  - 熱區色階（代表 `fishing_hours` 真實數值大小）維持嚴謹的連續藍色色階，
    不能為了可愛換成粉彩/彩虹漸層——那樣會讓深淺看不出數值強弱，等於犧牲
    網站存在的目的。
  - 裝飾層（吉祥物插畫、海龜/漁船圖示、海洋主題配色、圓角卡片、圓體字型）
    可以自由做可愛，只要不蓋住或混淆地圖上代表真實資料的圖層。
  - 驗收標準第 4 節模組 F 已加一條：可愛化裝飾不能讓人分不清「裝飾插畫」
    跟「地圖上真實定位資料點」。

---

## 3. 已知陷阱清單（最容易出隱蔽錯誤的地方，agent 必讀）

1. **經度慣例不一致（全專案第一大坑）**
   - OISST 原生是 0~360；GFW 與 Movebank 多為 -180~180。
   - 統一規則：任何來源進來先過 `align.normalize_lon()` 轉成 -180~180。
   - 症狀：疊圖看起來有東西、程式不報錯，但海龜點其實左右翻轉、跟漁船錯位。

2. **Movebank 欄位命名不一致**
   - 有的 study 叫 `location-long`，有的叫 `location_long`。務必寫欄位映射層，
     不要假設欄名。

3. **Argos 定位雜訊**
   - 海龜多用 Argos 標籤（非 GPS），精度粗、有雜訊點與重複時間戳。
   - MVP：先丟重複時間戳、丟 bbox 外的點即可。Douglas filter 列 v2。

4. **時區**
   - Movebank 時間戳可能非 UTC，讀入一律轉 UTC 再分箱。

5. **對齊要用 outer join**
   - 「有漁船無海龜」和「有海龜無漁船」兩類格子都要保留，
     它們正是「迴避/重疊」訊號所在。用 inner join 會把訊號 join 掉。

6. **憑證不得流經對話**
   - GFW token 只放 `.env`（已被 .gitignore 擋）。
   - 絕不寫死在程式碼、不印出、不貼進任何 AI 對話框。
   - （本專案開發過程中曾因把 token 貼進對話而作廢重發一次，引以為戒。）

---

## 4. 認識論主軸（這個專案為什麼有價值）

本專案的骨架是一個可反覆套用的推論結構：

> **當「宣稱指標」與「代理指標」出現裂縫時，裂縫本身就是分析標的。**

在此專案的投影：
- 不看漁業或保育機構「宣稱」的衝突熱區，看海龜軌跡與漁船作業的**實際空間交疊**。
- 「重疊」是行為訊號，不能直接等同「衝突/傷害」——要追到機制層（混獲）才是真相。

這與更大的方法論一致：**行為 → 意圖 → 機制**，不可跳步。
被動、未經表演的行為軌跡（海龜不知道自己被觀察）真實性高於任何自我陳述——
這也是為什麼這類資料吸引人。

**若分析結果與假說相反**（例如海龜與漁船呈正相關），
這不是失敗，是最有價值的時刻：要人來判斷是 bug 還是真實反直覺現象，
**不由執行/驗證 agent 自行改假說**。

---

## 5. 角色分工與交接協定

- **構思層（人 + Claude 對話）**：維護 spec.md 的假說與驗收標準、判斷資料揭露的意外。
- **執行層（Codex）**：唯一動程式碼的角色。依 spec 實作 src/ 模組，讓程式跑得動。
  一次做一個模組，做完停下等人確認。
- **驗證層（Cowork）**：不寫程式，拿 spec 第 4 節驗收標準查 Codex 產出是否為真，
  結果寫 findings.md，FAIL 附證據。
- **橋樑 = repo 裡的檔案**，不是人肉傳話。spec.md（命令）、程式碼（產出）、
  findings.md（裁決）、git log（軌跡）。三個 agent 不直接對話，只讀寫同一個 repo。

**Cowork 進場時機**：MVP 資料管線跑出實際結果（疊圖 + 相關係數）之後，
不是在空鷹架階段。空管線沒東西可驗，有結果的管線才驗得出隱蔽錯誤。

**模組 F 上線後的 git 權限邊界（2026-07-27 確認，與部署規則同一原則）**：
網站已部署（見第 6 節），repo 現在有公開的 GitHub remote。Codex 重新跑
`export_web_data.py` 產生新的 `outputs/web/*.json` 後，**可以自己
`git add` + `git commit`（本機、可逆，不影響任何人看到的東西）**，但
**不可以 `git push`**——push 會讓變更立刻出現在公開網址上，等於繞過
「實際對外發布需人確認」這條規則，也繞過了這個專案一路建立的審查機制
（本專案至今在網站這塊就攔到過六個問題：內嵌重複邏輯、analyze.py 沒真的
做完、可愛化沒做、地圖渲染壞掉、33MB 檔案沒分頁、Jekyll 蓋掉首頁——
都是先攔下來才修的）。push 這一步維持由人或構思層在審查後執行。

---

## 6. 目前進度與下一步

- [x] 階段0：GFW token 申請完成，放入 .env
- [x] 階段1：Codex 建目錄結構 + config.py
- [x] 階段2：Codex 實作 fetch_gfw.py。已用真實 API 跑過一個月連線測試
      （`data/raw/gfw_effort.parquet`，8134 格，BBOX 內，fishing_hours 有值）。
      2026-07-26 構思層（Claude Code）審查發現內嵌重寫 align.py 三個共用函式
      （normalize_lon/to_grid/to_time_bin）而非 import，已直接修正，記錄見
      findings.md 對應段落，`align.py` 本身未動。
- [x] study_id 選定：`1417866900`，日期範圍調整為 2017-01-01~2018-12-31。
      決策理由見第 2 節「Movebank study 選定」，已寫入 `src/config.py`。
- [x] 階段3：Codex 實作 `fetch_movebank.py`，從 `src/config.py` 讀取
      `STUDY_ID`，完成欄位映射、UTC 轉換、經度正規化、日期/BBOX 過濾與
      `(individual_id, timestamp)` 去重。已用公開 CC0 資料跑通：
      `data/raw/movebank_1417866900.parquet` 共 9240 筆、8 隻個體，物種全為
      `Caretta caretta`，通過 spec 模組 A 自動契約檢查。
- [x] 2026-07-26 構思層審查 `fetch_movebank.py`：PASS，不需修改。
      核對重點：欄位映射層正確處理 `location-long`/`location_long` 等別名；
      經度一律呼叫 `align.normalize_lon()`（未內嵌重寫，吸收了 fetch_gfw.py
      先前的教訓）；日期/BBOX 過濾與 `(individual_id, timestamp)` 去重順序正確
      （先正規化經度才比對 BBOX，避免 0~360 慣例來源誤判）；輸出契約（欄名、
      型別、經緯度範圍、無重複）人工重跑核對與程式內建 `_validate_contract`
      一致。species 100% `Caretta caretta`（Cheloniidae，符合驗收標準 A）。
      個體/時間分布與預期吻合：8 隻個體、9240 筆，時間落在 2017-01-01~2018-12-31。
      額外發現並認可的合理設計：Codex 加了「無 Movebank 帳密時，改抓
      Data Repository 的永久 CC0 公開檔案」的 fallback，且已誠實記在 README
      「實作假設」——這不是規格內建的方法，但因為選定的 study 本來就是為了
      不需個別索取才選的 CC0 資料，這個 fallback 沒有改變分析會用到的資料本身，
      判定為合理的工程選擇，不需退回。
- [x] 完整 GFW 資料已重新執行：以 31 天順序分塊完成
      2017-01-01~2018-12-31 全區下載，`data/raw/gfw_effort.parquet`
      共 266344 個週網格，`fishing_hours` 總和 20738897.443333，
      通過 spec 模組 B 自動契約檢查。
- [x] 2026-07-26 構思層審查 `fetch_gfw.py` 重跑結果：PASS。
      核對重點：`_date_chunks()` 以 31 天為單位切割 2017-01-01~2018-12-31、
      無重疊無缺口；所有分塊的原始逐日資料先 `pd.concat` 再統一
      `groupby(lon_bin,lat_bin,time_bin)`，分塊只影響 API 請求方式，不影響
      聚合正確性。實際重跑輸出核對：266344 個(格,週)、lon_bin/lat_bin 全部在
      BBOX 內、fishing_hours 無負值、無重複 (lon_bin,lat_bin,time_bin)，
      通過 spec 模組 B 自動驗收標準。time_bin 最早落在 2016-12-24（略早於
      DATE_START）是 `align.to_time_bin()` 週一起始的正常行為，海龜側會用
      同一函式分箱，不會錯位。
- [x] 2026-07-27 Codex 完成 `analyze.py`，已解決前次「檔案不存在」狀態。
      透過 `src.align` 既有函式產生 `data/interim/aligned.parquet`
      （267580 個格週），outer join 自檢通過；海龜點與漁船小時在對齊前後守恆。
      `outputs/figures/overlay.png` 已完成實際渲染與視覺 QA；
      `outputs/tables/correlation.csv` 結果為 Spearman ρ=-0.109186、
      p 值浮點下溢為 0、n=267580，顯示弱但統計顯著的負空間關聯。
      終端結論明確標示「不代表因果關係」。另澄清前項審查紀錄的最早週鍵：
      實際為 2016-12-26（不是 2016-12-24），由 `align.to_time_bin()` 產生，
      不影響兩來源對齊。
- [x] 2026-07-27 構思層審查 `analyze.py`：PASS。不是只看 Codex 回報的數字，
      獨立重新驗算：`turtle_points` 加總(9240) = movebank 原始列數；
      `fishing_hours` 加總(20738897.443333) = gfw_effort 原始加總，分毫不差；
      隨機抽一格(172.25°E, 34.25°N, 2017-05-15 那週)，不呼叫 align.py 程式碼、
      自己手刻經度正規化+網格化+週分箱重新計算，結果與 aligned.parquet 完全吻合
      （3 筆定位、1.6056 小時）——這是 spec 驗收標準 C 指定「抓對齊錯位的唯一
      可靠方法」。outer join 自檢：265476 格有漁船無海龜、1239 格有海龜無漁船、
      189 格兩者皆有，符合模組 D 設計。`calculate_correlation()` 只對
      `[fishing_hours, turtle_points]` 兩欄 dropna，沒有對整張表 dropna，
      避開了「sst/sst_anom 在 MVP 全是 NaN、對全表 dropna 會把 26 萬列全刪光」
      的陷阱。疊圖視覺印象（海龜軌跡形狀吻合已知蠵龜洄游模式，無經度翻轉跡象）
      與 Spearman 結論方向（弱負相關）不衝突，通過 spec 驗收標準 D。
      **兩個附帶發現（不影響 PASS，記錄供後續參考）**：
      (1) `align.py` 頂端 `GRID_DEG`/`TIME_BIN` 是寫死常數，其自身註解寫著
      「這些之後從 src/config.py import，骨架先放預設值」，但一直沒接上——
      目前兩邊數值剛好都一樣所以沒出事，但屬於陷阱清單同類的「兩份副本沒人
      同步」風險。`align.py` 屬於「勿改邏輯」核心模組，此項留給人決定是否要修，
      不由構思層自行動手。
      (2) n=267580 樣本量極大，ρ=-0.109 換算約只解釋 1.2% 變異，是很弱的效應；
      且格子間（相鄰週、相鄰網格）不獨立，p 值顯著程度可能被稀釋的自相關高估。
      結論文字已避免因果措辭，但正式報告/網站呈現時建議額外註明「效應量很小」，
      不要讓「統計顯著」給人比實際證據更強的印象。
- [x] **模組 F「接真資料」阻塞已解除**（2026-07-27）：上面 analyze.py 已審查 PASS，
      `src/export_web_data.py` 與把真資料接上前端現在可以開始做。
- [ ] 階段4：Cowork 依 spec 第 4 節獨立驗證，寫 findings.md（仍應進行，不因構思層
      審查 PASS 而跳過——構思層審查與 Cowork 獨立驗證是兩層不同的查核）
- [ ] 回構思層：若全 PASS → 做 v2（加 SST）；若相關方向與假說相反 → 人判斷
      （**不由 Codex/Cowork 自行改假說，出現此情況要在 findings.md 標記並停下**）
- [~] 2026-07-27 構思層審查 `web/` 前端骨架：**部分 PASS，可愛化未做，需 Codex 補**。
      技術/資料嚴謹面確認良好：`FISHING_PALETTE` 逐字元對照 spec 的 13 階藍色
      色票、順序正確；海龜紅色 `#e34948` 正確；圖例、hover tooltip、click popup、
      時間滑桿、圖層開關、Spearman 結果面板、免責文字（相關不等於因果、個位數
      個體警語）都有實作且功能正確；配色計算是用整個假資料集的 min/max 算出
      全域色階（不是每週各自重新計算），這樣拖時間滑桿時圖例才有意義，處理得對；
      無障礙細節（ARIA label、focus-visible）、RWD 斷點、Leaflet CDN 用 SRI hash
      都有做，程式碼品質好。
      **但完全沒做「可愛化」**——spec.md 模組 F 那五項可愛化要求（海龜圖示取代
      純色點、漁船圖示、海洋主題裝飾/波浪/圓角以外的暖色調、吉祥物插畫、標題用
      圓體字）一項都沒實作。海龜點仍是純色 `L.circleMarker`，沒有小船圖示、沒有
      吉祥物、字體全程 system-ui。目前畫面是乾淨專業的儀表板風格，不是人要求的
      「可愛」。這不是我自己主觀覺得不夠可愛，是對照 spec.md 明文列出的五項清單
      逐項核對後發現全數缺漏，屬於未完成，不是風格建議。
      **另一個留意事項（非本次阻塞項，供之後接真資料時參照）**：`app.js` 目前
      把 `GRID_DEG = 2.5` 寫死在檔案頂端給假資料用；接上真正的 `grid.json` 後
      這個值不能繼續寫死複製一份，應該從資料或設定動態帶入，否則就是本專案
      第三次出現「同一個常數兩份副本沒人同步」的模式（前兩次是 fetch_gfw.py
      的經度公式、align.py 的 GRID_DEG/TIME_BIN）。
- [x] 2026-07-27 構思層審查可愛化補做：**PASS**（附兩個新發現的 bug，見下）。
      逐項對照 spec.md 模組 F 的五項可愛化清單：海龜圖示 ✅（🐢 emoji 取代純色點，
      大小仍隨定位次數變化）、漁船圖示 ✅（圖例與載入畫面的 ⛵，有跳動動畫）、
      海洋主題裝飾 ✅（頁首吉祥物徽章、波浪形分隔線、卡片圓角加大、柔和漸層）、
      吉祥物 ✅（頭像式徽章 + 「🐢 小龜提醒你」把因果免責聲明擬人化，處理得好）、
      圓體字 🟡 部分（標題改 Trebuchet MS，非 Nunito/Quicksand 那種圓潤字體，是
      為了不載外部字體的權宜選擇，不算錯但可愛感較弱，非阻塞）。`FISHING_PALETTE`
      色階原封不動，紅線守住。
      同一次 Codex 也主動把 `src/export_web_data.py` 做完並接上真資料（此前已
      解除阻塞），`app.js` 的 `GRID_DEG` 也已改為 `0.25` 常數（仍是寫死，但至少
      跟 config 對齊；理想上應動態帶入，非本次阻塞項，留意即可）。
      **實際啟動本機伺服器、用無頭瀏覽器開啟頁面驗證（不是只看程式碼）後，
      發現兩個需要 Codex 修的問題，發布前必須解決**：
      1. **地圖圖層沒有渲染出來（bug，優先）**：地圖磚塊呈現破碎、大小錯位、
         無法拼接成連續地圖的樣子；完全看不到漁船熱區色塊、也看不到海龜圖示，
         等了 12 秒重新截圖畫面仍相同，不是載入中的暫態。右側 Spearman 結果
         面板數字正確（ρ=-0.109、n=267580、8 隻個體），代表資料抓取與
         `export_web_data.py` 沒問題，問題只在地圖圖層渲染。症狀很像 Leaflet
         常見地雷：地圖容器在 CSS grid/flex 版面還沒排定最終尺寸前就被
         `L.map()` 初始化，導致內部磚塊座標算錯；通常在版面穩定後呼叫一次
         `map.invalidateSize()`（例如監聽版面穩定的時機，或至少在
         `loadData()` 資料就緒、`loadingState` 隱藏之後呼叫一次）可以修。
         請 Codex 檢查並修正，修完後要在瀏覽器實際看過畫面再回報完成，
         不能只憑程式碼推斷。
      2. **`outputs/web/grid.json` 高達 33MB，不分頁一次全部塞給瀏覽器**：
         26.7 萬列一次性 JSON 陣列，公開網站上這個大小會嚴重拖慢載入，尤其
         行動網路。畫面的時間滑桿每次只顯示「一週」的資料，卻要求瀏覽器先
         抓下整個兩年份資料才能開始互動，不合理。建議二選一（Codex 自行判斷
         哪個實作成本較低）：(a) 依週拆成多個小檔案（例如
         `outputs/web/weeks/2017-01-02.json`），前端隨滑桿即需即抓；
         (b) 至少改成欄位式（columnar）JSON 而非逐列重複鍵名的物件陣列，
         大幅縮小檔案。不管哪種，確認靜態託管平台會用 gzip/brotli 壓縮也有幫助，
         但不能只靠壓縮，26.7 萬列資料本來就不該一次全部載入只顯示一週的畫面。
- [x] 2026-07-27 構思層審查兩個 bug 修復：**PASS**。實際重開瀏覽器驗證（不只看
      程式碼）：地圖磚塊正常拼接、熱區色塊與海龜圖示都正常渲染；`app.js` 改為
      先抓輕量 `weeks.json` manifest，滑桿移動時才即需即抓對應那一週的小檔案
      （`weekCache` 做快取避免重複抓取），`GRID_DEG` 也已從 `src.config` 動態帶入
      `weeks.json` 的 `grid_deg` 欄位，不再另外寫死複製一份；地圖渲染 bug用
      `map.invalidateSize()`（初次資料就緒後 + 加了 `ResizeObserver` 持續監聽版面
      尺寸變化）修正，比原本建議的做法更周全。
      **一個順手處理的殘留問題**：`export_web_data.py` 仍會另外輸出一份完整的
      `outputs/web/grid.json`（33MB），拆成逐週檔案後這份已經沒有任何地方在用，
      放著會不必要地塞大小進部署內容。這次沒有請 Codex 改程式碼，而是用
      `.gitignore` 直接排除這個檔案（`outputs/web/grid.json` 明確排除，
      `outputs/web/` 其餘檔案維持追蹤），不影響 export_web_data.py 本身的邏輯。
      之後 Codex 若要順手清掉這段沒人用的輸出程式碼也可以，非必要。
- [x] **2026-07-27 網站已發布上線**：
      `https://evan1206.github.io/turtle-fishing-sst/web/`（GitHub Pages，
      repo `Evan1206/turtle-fishing-sst`，public，master 分支根目錄）。
      部署前處理事項：
      - 發現 `.gitignore` 原本整個排除 `outputs/`，會導致網站運作必需的
        `outputs/web/summary.json`、`weeks.json`、`weeks/*.json` 全部沒有
        進版本控制、部署後網站會顯示「資料載入失敗」——已修正為
        `outputs/*` + `!outputs/web/`（`outputs/figures`、`outputs/tables`
        等可重算產物維持排除）。
      - 部署前掃過所有將被追蹤的原始碼檔案，確認沒有硬編碼的 GFW/Movebank
        憑證字串；`.env` 本身仍在排除清單內。
      - 部署完成後**直接對公開網址重新截圖驗證**，畫面與本機測試一致
        （地圖、熱區、海龜圖示、Spearman 結果面板皆正常），不是只憑
        HTTP 200 就判定成功。
      - repo 是這次才用 `git init` + `gh repo create` 新建的（先前這個資料夾
        沒有版本控制），使用者已在此之前用 `gh auth` 登入帳號 `Evan1206`。
- [x] **2026-07-27 修正部署後的根網址陷阱**：人回報打開頁面「完全沒有圖示只有
      一堆程式碼」——查出來是 GitHub Pages 預設用 Jekyll 處理，沒有根目錄
      `index.html` 時會把 `README.md` 自動轉成首頁；README 裡有好幾段
      PowerShell 安裝指令的程式碼區塊，Jekyll 渲染出來看起來就是「一堆程式碼」，
      沒有地圖、沒有海龜——**網站本體其實沒壞，是根網址被 Jekyll 蓋掉了**。
      修法：新增根目錄 `.nojekyll`（停用 Jekyll 處理）與根目錄 `index.html`
      （0 秒 meta-refresh 導到 `./web/`，並附一個手動連結防止導轉失敗）。
      推上去、等 Pages 重新 build 後，**對根網址重新截圖驗證**，確認會自動
      跳轉到正確的儀表板畫面，跟 `/web/` 顯示的內容一致。
- [x] 2026-07-27 構思層審查「如何判讀」說明區塊：**PASS**。用 `<details>/<summary>`
      原生可展開元件實作（語意正確、可鍵盤操作），只改了 `web/index.html` 與
      `web/style.css`，沒碰 Python 端，符合預期。實際在瀏覽器裡點開驗證（不只看
      程式碼）：四點都有寫、內容跟 spec 要求逐字對得上——網格意義（0.25°×1週，
      顏色是那格那週的作業小時非累計量）、海龜大小意義（定位次數非海龜數量/
      體型）、ρ 怎麼讀（排序相關非百分比）、滑桿意義（切換靜態週次非動畫）。
      開頭有一句話把這區塊跟右側的因果免責提醒明確區分開，不會讓人搞混兩者。
      視覺風格（小龜吉祥物、圓角卡片、藍/薄荷漸層、展開時 ＋ 轉 × 的圖示動畫）
      跟全站一致。Codex 有照規則只 commit 沒有 push。
      **本次沒有發現需要退回的問題。**
      未做但也未要求的：commit 訊息、視覺細節都合理，暫無後續待辦。

**MVP 邊界**：模組 A、B、D、E（無 SST）。目標產出：一張疊圖 + 一個 Spearman 結論。
模組 F（網站）是 MVP 之外的發布層，見上方版本邊界說明與 spec.md 第 6 節。

---

## 7. 版本邊界

- **MVP**：海龜 × 漁船的空間關聯（無 SST）。
- **v2**：加 OISST 海溫，做機制層（分布中心是否隨海溫距平同向移動）；加 Douglas filter。
- **v3**：多海域比較、時間動態。
